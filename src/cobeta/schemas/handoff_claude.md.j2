# {{ spec.name }} (cobeta workspace)

This directory is a **cobeta-generated ICM workspace**. The folder layout is
intentional, designed by a bootstrap agent that consulted your past project
conventions. Stick to it; don't sprawl new top-level folders.

## What this workspace is

> {{ spec.intent }}

{% if spec.rationale -%}
**Why this layout:** {{ spec.rationale }}
{% endif %}

## Folder layout

{% macro render_cell(cell, depth=0) -%}
{{ '  ' * depth }}- `{{ cell.name }}/` — {{ cell.purpose }}{% if cell.expected_structure %} ({{ cell.expected_structure }}){% endif %}

{% for sub in cell.sub_cells -%}
{{ render_cell(sub, depth + 1) }}
{%- endfor %}
{%- endmacro -%}
{% for cell in spec.cells -%}
{{ render_cell(cell) }}
{%- endfor %}

Every cell folder has its own `CONTEXT.md` with declared inputs / outputs.
Read the cell's CONTEXT.md before doing work inside it.

## Tags in scope

{{ spec.tags | join(', ') if spec.tags else '(none — declare in viking://meta/tags.yaml before using)' }}

## Memory plan (where to put what)

{% if spec.memory_sections -%}
| Topic | URI | When to write |
|---|---|---|
{% for m in spec.memory_sections -%}
| {{ m.purpose }} | `{{ m.uri }}` | {{ m.write_pattern }} |
{% endfor %}
{%- else -%}
All workspace memory: `{{ viking_memory_uri }}`
{%- endif %}

## Discipline (read before sprawling)

- **Never create a new top-level folder** without first amending this
  workspace's `CONTEXT.md`. The layout is the design; ad-hoc dirs erode it.
- When you're tempted to add `tmp/`, `misc/`, `scratch/` — instead, find which
  existing cell that file belongs to. If none, ask the human.
- Cross-machine: this workspace's default machine is `{{ spec.machine }}`. If
  you need an artifact from elsewhere: `cobeta exec <other-machine> -- ...`.

---

> Auto-written by cobeta {{ cobeta_version }}.
