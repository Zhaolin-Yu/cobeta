---
description: cobeta workspace — {{ spec.name }}
globs: ["**/*"]
alwaysApply: true
---

# {{ spec.name }}

> {{ spec.intent }}

Layout (don't sprawl):
{% for cell in spec.cells -%}
- `{{ cell.name }}/` — {{ cell.purpose }}
{% endfor %}

Each cell has its own CONTEXT.md. Read it before doing work in that cell.

Tags: {{ spec.tags | join(', ') if spec.tags else '(none)' }}
Memory root: {{ viking_memory_uri }}
Default machine: {{ spec.machine }}
