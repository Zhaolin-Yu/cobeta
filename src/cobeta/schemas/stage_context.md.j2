# Stage {{ stage.id }} — {{ stage.name }}

> {{ stage.purpose }}

## Inputs

{% if stage.contract.inputs -%}
| Source | File / Location | Why |
|---|---|---|
{% for src, loc, why in stage.contract.inputs -%}
| {{ src }} | `{{ loc }}` | {{ why }} |
{% endfor %}
{%- else -%}
_No inputs declared yet. Add rows when you know what this stage needs._
{%- endif %}

## Process

{% if stage.contract.process -%}
{% for step in stage.contract.process -%}
{{ loop.index }}. {{ step }}
{% endfor %}
{%- else -%}
_Define the steps before starting work._
{%- endif %}

## Outputs

{% if stage.contract.outputs -%}
{% for out in stage.contract.outputs -%}
- {{ out }}
{% endfor %}
{%- else -%}
_What does this stage produce? Fill in before working._
{%- endif %}

## Audit (before exiting this stage)

{% if stage.contract.audit -%}
{% for item in stage.contract.audit -%}
- [ ] {{ item }}
{% endfor %}
{%- else -%}
- [ ] Outputs match the declarations above
- [ ] Any deviation from inputs noted in `output/_log.md`
{%- endif %}
